import { SearchfunctionlistPipe } from './searchfunctionlist.pipe';

describe('SearchfunctionlistPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfunctionlistPipe();
    expect(pipe).toBeTruthy();
  });
});
